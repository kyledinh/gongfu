'use strict';

/* jasmine specs for controllers go here */

describe('Beta controllers', function() {
 
  describe('BetaCtrl', function(){
 
    it('should use the DataService to get message', function() {
      /*
      var scope = {},
          ctrl = new BetaCtrl(scope);
          expect(scope.data.message).toBe("I'm for a service");
       */
    });
  });
});
